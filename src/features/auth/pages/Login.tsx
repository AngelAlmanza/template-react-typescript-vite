import { Container, Typography } from "@mui/material";

const Login = () => {
  return (
    <Container maxWidth="xl">
      <Typography
        variant="h1"
        color="text.primary"
      >
        Login
      </Typography>
    </Container>
  );
};

export default Login;
